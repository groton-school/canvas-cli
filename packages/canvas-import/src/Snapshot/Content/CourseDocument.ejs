<% if (content.Content.length > 0) { %>
<div>
    <h3><%= documentType %></h3>
    <% content.Content.forEach(item => { %>
    <% if (item.AttachmentQueryString) { %>
        <div><small><%- include('CanvasFileLink', {file: item.AttachmentQueryString.canvas}) %></small></div>
    <% } %>
        <div><%- item.Description %></div>
    <% }); %>
</div>
<% } %>
